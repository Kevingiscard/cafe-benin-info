{
  "name": "cafe-benin-info",
  "version": "4.0.0",
  "description": "Café Bénin — encyclopédie et dictionnaire francophone du café, avec dossier documentaire sur le Bénin",
  "private": true,
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "npx vercel dev",
    "validate": "node scripts/validate.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "npm run validate && npm test"
  },
  "dependencies": {
    "@google/genai": "^2.13.0",
    "@supabase/supabase-js": "^2.39.0",
    "nodemailer": "^9.0.3"
  }
}

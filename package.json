{
  "name": "cafe-benin-info",
  "version": "1.0.0",
  "description": "Café Bénin V1.0 — plateforme documentaire statique francophone sur le café et le Bénin",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "test": "node --test tests/*.test.mjs",
    "check:assets": "node scripts/check-assets.mjs",
    "check:a11y": "node scripts/check-a11y.mjs",
    "check:links": "node scripts/check-links.mjs",
    "check:links:external": "node scripts/check-links.mjs --external",
    "check:seo": "node scripts/check-seo.mjs",
    "check:security": "node scripts/check-security.mjs",
    "check:size": "node scripts/check-size.mjs",
    "health": "node scripts/health-check.mjs",
    "build": "npm run validate && npm test && npm run check:assets && npm run check:size && npm run check:a11y && npm run check:seo && npm run check:security",
    "verify": "npm run build && npm run check:links"
  }
}
